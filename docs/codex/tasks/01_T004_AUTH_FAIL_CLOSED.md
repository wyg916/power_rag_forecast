你正在处理“智能运营分析项目”。请严格遵守项目根目录 `AGENTS.md`、`docs/codex/PROJECT_BASELINE_SUMMARY.md`、`docs/codex/MASTER_ENGINEERING_RULES.md`，以及本任务文件。

执行要求：
1. 不重复全仓扫描，优先精确定位。
2. 任何修改前先建立任务级检查点并写入项目盘。
3. 不在 C 盘新增、删除或修改项目文件。
4. 不删除、批量移动或覆盖已有文件；需要时停止等待用户确认。
5. 回复保持简洁，完整证据写入 `docs/codex/evidence/`。
6. 遇到停止条件时不要变通，直接列出需要用户确认的事项。

# 任务：T004 — 认证与敏感配置改为生产 fail-closed

## 目标
统一前后端认证默认值；生产环境缺少 JWT/管理员必要配置时拒绝启动；完成普通用户、管理员和调试角色的关键负向测试。

## 范围
仅修改认证、配置加载、启动校验、相关前端认证契约和对应测试。不要修改模型、预测、RAG、报告和页面样式。

## 强制要求
- 开始前建立 T004 检查点。
- 不写入真实 secret；测试使用测试专用占位值。
- 不修改 Windows 全局环境变量或 C 盘配置。
- 不全局安装依赖；如缺依赖，先列出并请求确认。

## 实施要点
1. 明确 development/test/production 三种环境语义。
2. production 下强制 `AUTH_REQUIRED=true`。
3. production 缺 JWT secret、管理员初始化条件或其他必要配置时启动失败，并返回明确错误。
4. 移除默认弱密钥和默认管理员密码回退。
5. 统一前后端 401、403、Token 过期和 SSE/导出认证。
6. 保持健康检查和构建不回退。

## 验收测试
- production 缺 secret：启动失败。
- 未登录访问保护接口：401。
- 普通用户访问管理员接口：403。
- 管理员允许访问授权操作。
- 日志、SSE、导出不出现 Token、密码、连接串或内部 Trace。
- 原有 FastAPI health、React typecheck/build 通过。

## 输出
只给结论、修改文件、测试摘要、回滚路径和剩余风险；完整日志写证据目录。
