你正在处理“智能运营分析项目”。请严格遵守项目根目录 `AGENTS.md`、`docs/codex/PROJECT_BASELINE_SUMMARY.md`、`docs/codex/MASTER_ENGINEERING_RULES.md`，以及本任务文件。

执行要求：
1. 不重复全仓扫描，优先精确定位。
2. 任何修改前先建立任务级检查点并写入项目盘。
3. 不在 C 盘新增、删除或修改项目文件。
4. 不删除、批量移动或覆盖已有文件；需要时停止等待用户确认。
5. 回复保持简洁，完整证据写入 `docs/codex/evidence/`。
6. 遇到停止条件时不要变通，直接列出需要用户确认的事项。

# 任务：T002 — 安全模型加载与严格特征契约

## 目标
对唯一 Candidate 建立 artifact manifest/hash 校验、隔离加载流程、严格 170 项特征契约和固定 24 行推理验证；任何契约异常必须 fail-closed。

## 重要停止条件
**在反序列化任何 joblib/pickle 前必须停止并请求用户明确授权。** 未获授权时只完成静态 manifest、hash、schema 与测试框架，不得变通加载。

## 范围
模型 artifact 校验器、加载器、特征契约、固定输入测试和模型准入服务。不要激活模型、不要写正式业务库、不要修改前端。

## 强制要求
- 无网络、只读模型目录、隔离输出目录。
- 不在 C 盘创建模型缓存、环境或输出。
- 禁止缺列静默补 0、额外列忽略、跨版本 fallback。
- Candidate 验证通过后仍保持 validated，等待人工确认才允许 Active。

## 实施要点
1. 生成/校验 artifact manifest：文件 hash、模型版本、特征版本、schema hash、运行时版本、domain/target。
2. 建立严格 schema：列名、顺序、类型、时区、连续小时、空值、有限数值。
3. 对三项误差记忆占位特征给出明确处理：正式实现或从下一版本移除；不得继续无声明占位。
4. 获授权后在隔离环境完成固定 24 行推理，保存输出 hash 和性能摘要。
5. 建立准入状态及失败原因，不自动激活。

## 验收测试
正常输入成功；缺列、多列、顺序错误、dtype 错误、时区缺失、时间不连续、hash 错误、feature_version 错误均失败；失败无数据库副作用；未授权环境安全跳过。

## 输出
先完成静态部分；到反序列化步骤必须停止并请求授权。授权后继续并提交证据。
