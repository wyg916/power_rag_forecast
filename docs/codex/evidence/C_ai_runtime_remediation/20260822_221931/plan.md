# C Remediation Closure plan

1. Gate exact worktree/branch/HEAD/clean and create a project-disk checkpoint.
2. Close attachment POST/GET/DELETE, terminal cleanup, ownership, audit, citation, TTL and reuse denial.
3. Diagnose and repair OpenAI-compatible content parsing, MiMo Vision and DeepSeek AnalysisPlan structured output.
4. Prove the existing semantic catalog and at least 20 Golden queries without database writes.
5. Reproduce and classify the previous 50 full-suite failures without deleting tests or broadening skips.
6. Re-run C, startup, CI, migration, permission, safety and real-provider gates; report and commit clean.

No frontend, prediction engine, model ops, migration, database write, model activation, RAG alias, unknown port termination, merge, rebase, tag or new worktree is authorized.
