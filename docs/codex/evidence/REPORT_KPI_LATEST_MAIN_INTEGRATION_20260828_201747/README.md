# 报告中心指标卡合入最新主项目

## 结论

报告范围 `PASS`。以 `main@307e0b9730e8a58b713ca328975de5ffcc777cb8` 为目标基线，语义合入已审核的报告中心布局链：

- `12b05be0f47a1761d364ffdf0318f17cec7eecf6`
- `c76747ee807d8e5d78e85a392fbacfaaecfeee3e`
- `1d92e8b04d299c2339d8803102454b2f9b2c0501`
- `6e91ea8e032c57133dab9b9195966e65bb0a7018`

未整分支合并。只接收报告中心页面结构、专属 CSS、报告契约测试和专项 pytest，并保留最新主项目已经存在的 `canPerformAction` 权限门禁、对象值安全渲染、无权限写操作隐藏和真实 API 参数。

## 验收摘要

- TypeScript：PASS。
- Vite 生产构建：PASS，3,690 modules。
- 报告专项 pytest：13 passed，2 skipped；跳过项均因未配置 `PHASE5_C_TEST_DATABASE_URL`。
- 前端报告契约：4/4 PASS。
- 前端全量单元契约：56/57；唯一 10px 全局视觉门禁在未修改 `main` 上同样失败，非本次引入。
- 扩大 pytest：71 passed、2 skipped、3 failed；三个失败分别属于策略文案、首页 stale 标记和权限矩阵快照，并已在未修改 `main` 同项复现。
- 浏览器：日报、审核发布两页均从真实接口加载；1280×720、DPR=1 下文档横纵溢出均为 0，8/8 指标说明完整显示，数值/说明垂直中心差 0px，组合水平中心最大误差 0.008px，控制台 warning/error 为 0。
- 只读交互：切换第二份报告后，左侧 active report ID 与中间预览 report ID 完全一致。

## 功能边界

生成、下载、审核、驳回、重新生成、发布的处理器、权限和后端受保护 API 均保留并通过静态契约。为避免修改正式报告和审核状态，浏览器验收未点击生成、审核、驳回、发布等写操作。

## 数据库影响

无迁移、无 Seed、无业务数据写入、无报告状态流转。
