# 预测中心三页面右侧返工候选证据

- 状态：`READY_FOR_USER_VISUAL_CONFIRMATION`；这是候选状态，不是最终 PASS。
- 唯一基线：`1f4f7e50e3177aec21594b52ce74cd03dd77ea7d`。
- 候选分支：`codex/ui-forecast-center-fidelity-20260814`。
- 候选 worktree：`E:\智能运营分析项目_worktrees\ui_forecast_center_fidelity_20260814`。
- 被拒绝候选 `bc8c4c568fe838cc6927890d0d6c20bb760fa730` 未合入、未继续修补。
- 数据库结构、模型状态、RAG、权限体系、后端文件修改：0。
- 远端推送、冻结 Release 修改、最终项目集成：0。

## 本轮边界

- 锁定且未修改：`App.tsx`、`BasicLayout.tsx`、`Sidebar.tsx`、`HeaderBar.tsx`。
- 仅修改预测中心三页右侧工作区的排版、文字层级、对齐、间距、图标、状态、表格列、图表与信息密度。
- 后端审计字段仍保留，右侧页面不展示过期原因、生成/更新时间、run_id、模型/特征版本、预测日期、区域、数据源、推理模型、适用窗口、批次状态或异常原因。

## 结果摘要

- 24 小时预测：业务摘要工具栏、5 个指标卡、正式区间判定、峰谷窗口、白底洞察分隔、8 行明细、紧凑状态卡。
- 峰谷/模型：多色分段峰谷仪表、白底业务解释、7 张工程评估卡、Baseline 对比与训练回测状态。
- 历史对比：近 7/30 天真实历史均值、小时/天/周粒度、三序列、关键时点标注、7 行小时明细与紧凑侧卡。
- 不生成业务假数据；置信区间只在接口逐点提供真实上下界时展示。
- 历史页已移除“数据状态 / 已过期”；三页右侧源码不再渲染用户禁止的审计、版本、批次和过期说明字段。
- 模型页底部在不改变外部 Card 高度和左右比例的前提下增大表格行高，并把训练状态与入口均匀铺满 Card 内部。

## 自动验收结论

- 三个 Figma 节点 `2:2`、`3:2`、`4:2` 已实际读取；目标图与候选图已在 1672×941、DPR=1 下逐页并排，并生成叠加/差分辅助图。
- 三页主视口均无横向溢出、无意外纵向页面滚动、无遮挡、无不可点击区域；1920×1080、1600×900、1440×900、1366×768 也无横向溢出。
- 真实 API/PostgreSQL 的 `run_id`、24 行及 min/max/avg 完全一致；验收窗口 54 个目标页请求全部为 HTTP 200。
- 三页 Console Error=0、Console Warning=0；逐项刷新、分页、粒度、范围、抽屉、导出、权限禁用态和跳转通过。
- Loading、Stale、Unauthorized、Forbidden 已在浏览器/真实接口验证；真实库存在 24 条有效记录，因此 Empty 采用现有状态契约验证而不修改数据库或伪造业务数据；真实后端中断时 Error/Retry 状态已在本任务恢复阶段验证。
- `run_project.bat` 连续两次 PASS；正式 build PASS。

## 用户确认与集成门禁

候选必须等待用户针对最终候选 SHA 和本目录截图明确确认。确认前禁止 merge、cherry-pick、rebase、复制到冻结 Release 或描述为最终项目已经改变。

回滚：在候选分支对本候选提交执行 `git revert <candidate-sha>`；冻结 Release 无需回滚，因为从未修改或集成。
