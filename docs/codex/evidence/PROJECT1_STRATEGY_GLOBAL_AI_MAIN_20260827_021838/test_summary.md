# 验收摘要

## 自动化

- `npm run typecheck`：PASS。
- `npm run lint`：PASS，12/12。
- `npm run unit`：PASS，53/53；包含全局 AI 挂载、会话状态、抽屉层级和 stale 元数据保留但不渲染的契约。
- 策略无写入测试子集：PASS，76 passed、12 deselected；未运行需要独立测试数据库或会触发受控写入的用例。
- `npm run build`：PASS，Vite 3688 modules transformed。
- `git diff --check`：PASS（仅存在 Windows 行尾提示，无空白错误）。

## 浏览器

- 1919×875：总览、储能、人工复核三页截图完成；页面级横向溢出 0；全局 AI 与业务控件重叠 0；可见过期类标签 0。
- 1440×900：三页横向/纵向页面溢出均为 0；AI 控件重叠 0；可见过期类标签 0。
- 1366×768：三页横向/纵向页面溢出均为 0；AI 控件重叠 0；可见过期类标签 0。
- 人工复核表格可见行高 42px；左右工作区各自内部滚动。
- 全局 AI：悬浮按钮可见并可打开抽屉；抽屉包含页面上下文、新会话、收起、附件、完整助手与发送入口。
- 复核功能：查看、人工审核和宽屏导出入口可用；批量通过/驳回继续保持原 disabled 门禁。
- 控制台：验收地址 warning/error 0。

## 数据影响

- 数据库写入：0。
- Migration/Seed：0。
- 审核状态流转、发布、自动交易、设备控制：0。
- 外部网络调用：0。
