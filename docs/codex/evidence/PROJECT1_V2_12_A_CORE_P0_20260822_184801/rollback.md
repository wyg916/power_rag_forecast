# 回滚

- Git：每个逻辑提交使用 `git revert <sha>`；禁止 reset/clean/切换分支。
- 文件：检查点保存了所有开工时关键文件副本和 SHA256；覆盖恢复前必须再次确认精确文件。
- 数据库：预测输入、run 和结果采用同事务写入，失败自动回滚为 0 半成品。
- 成功事实保留审计；如需撤销，只能在人工确认后按本任务精确 batch_id/run_id 做补偿事务。
- 结构：本轮开工前 schema-only 备份位于检查点；若新增迁移，必须提供 downgrade 并先在隔离 schema 验证。
- 报告/策略：不批量删除，使用既有状态机或保留为审计样本。
- 本轮未新增迁移、未切换 Active。成功样本 run 为 `run_20260822T115159818317Z_5370682c70`，报告和策略 ID 见总报告。
- 代码回滚后保留已生成事实用于审计；如最终集成负责人明确要求数据补偿，必须另开授权任务并按精确主键逐项执行，不在本任务删除。
