# PROJECT1_MODEL_CENTER_ACTIVE_MODEL_UI_LAYOUT_REFINEMENT

1. 核对正式最新 `main`、项目规则、P6 UI 技能、目标图片、模型中心源码、真实 API 与 RBAC。
2. 建立任务前检查点，记录 Git、关键文件哈希和数据库零影响。
3. 只修改模型中心页面结构类名与页面级 CSS；不修改服务、路由、权限、后端、数据库和处理函数。
4. 在登录态 Chrome 中以 1919×882、100% 缩放完成改前/改后、滚动、功能入口和多视口验收。
5. 完成 TypeScript、生产构建、前端单测、模型中心契约/API 测试及只读真实 API 冒烟。
