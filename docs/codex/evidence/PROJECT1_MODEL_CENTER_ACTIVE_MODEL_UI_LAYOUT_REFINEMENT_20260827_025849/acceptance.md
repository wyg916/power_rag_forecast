# Acceptance

STATUS: PASS

- 1919×882、100%：首屏完整，页面 max scroll 263px → 0px。
- 标题栏：106px 两层 → 60px 单行；标题为“模型中心 / Active 模型”。
- Sticky：全局头部 bottom 60px 与模型标题 top 60px 精确衔接；白色不透明背景、z-index 300；滚动目标 0/100/300/bottom 均无内容穿透。
- KPI：6 张卡统一 94px；Active/Candidate 名称与日期自然分层，不再任意断词。
- 图表：两卡统一 218px；图例、控制器、坐标轴、tooltip、series 与 ECharts autoResize 均保留；真实无数据时保持轻量空态画布，不伪造曲线。
- 模型表：8 列完整；1919×882 clientWidth=scrollWidth=901px，无横向滚动；窄屏仍由表格容器管理横向滚动。
- 治理区：准入/训练/评估/回滚 2×2，4/4 首屏完整可见。
- 回滚：长版本号省略并有 Tooltip；警示信息保留；卡片 body 右侧预留 78px，不与 AI 浮钮争用。
- 响应式：1920×1080、1600×820、1440×900、1366×768 的 body 水平溢出均为 0。
- 功能入口与 7 个处理函数前后一致；权限、隐藏、disabled、loading 条件未改。

明确未发生：接口变更、后端逻辑变更、数据库变更、RBAC 变更、处理函数删除、禁用/加载/权限可见性变化、训练/激活/回滚业务写入。
