# 策略中心三页 UI 独立评审版

- 状态：`READY FOR HUMAN REVIEW`，尚未合入 `main`。
- 评审分支：`codex/strategy-center-ui-review-v2`。
- 基线：`873a1cd3271150526b06c3160f02fb821e891982`。
- 修改边界：仅前端页面布局、视觉样式与前端契约测试；后端、API、数据库、权限、路由和业务写操作均未修改。
- 数据原则：储能设备概览、执行反馈摘要和人工复核列表全部来自现有 `GET /api/strategy/runtime-facts` 返回值及其前端只读聚合；没有前端硬编码、随机生成或数据库补数。

## 交付结果

1. 三个 Tab 改为等宽、高对比度的独立按钮组；策略日期和审核状态归入右侧业务上下文区域。
2. 总览主图与说明区、底部四卡占满有效宽度；四卡严格等宽，收益卡右边框与上方说明卡对齐；执行摘要改为完整环形进度和显式完成率。
3. 储能页设备卡、图表、执行表和右侧详情共同填满首屏；设备清单下方及表格下方使用现有接口数据生成只读摘要。
4. 人工复核页表体高度随视口自适应，现有 9 条记录在目标视口完整利用列表区；底部摘要和右侧详情保持独立滚动。
5. 全局 AI 悬浮按钮沿用现有全局组件和抽屉能力，并保留右下安全区。

## 人工审批门禁

本分支仅供截图和交互评审。只有用户明确回复“通过”后，才允许将本提交选择性合入最新主项目。
