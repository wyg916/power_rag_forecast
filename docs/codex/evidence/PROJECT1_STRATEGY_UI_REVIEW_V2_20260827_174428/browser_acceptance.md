# 浏览器验收记录

## 1919 × 928

- 文档 `clientWidth/scrollWidth = 1919/1919`，`clientHeight/scrollHeight = 928/928`。
- 总览：主区右边界 1888px；底部四卡均为 408px，最右收益卡与上方策略建议说明右边界均为 1888px。
- 储能：工作区高度 620px，左设备卡、中心列和右详情均填满工作区；现有 API 返回 2 台设备、24 个所选设备 SOC 点、3 条反馈，并显示 4 项执行摘要。
- 人工复核：工作区高度 620px；现有 9 条记录均进入表格有效区，底部摘要 118px；右侧详情为独立内部滚动。
- Tab 激活态背景为 `rgb(8, 122, 91)`，文字白色；三页入口均可点击。
- Global AI 浮钮可点击，`全局 AI 助手` 抽屉正常打开和关闭。

## 1440 × 900 / 1366 × 768

- 两档均满足文档 `scrollWidth = clientWidth`、`scrollHeight = clientHeight`。
- 1366×768 下三页根容器右边界 1335px、底部 746px；总览主区、底部四卡、储能工作区和复核工作区均位于有效视口内。
- 1366×768 下人工复核表格出现组件内横向滚动条，未把滚动责任转移到整页。
- 全局 AI 浮钮在 1366×768 下为 44×44px，位于右下安全区。

## 交互

- 两台储能设备可往返切换，SOC 和摘要随所选设备联动。
- 刷新按钮启用且刷新后业务数据恢复成功态。
- 人工复核搜索可过滤到目标编号，清空后恢复全部记录。
- 浏览器 console 的 warning/error 均为 0。
