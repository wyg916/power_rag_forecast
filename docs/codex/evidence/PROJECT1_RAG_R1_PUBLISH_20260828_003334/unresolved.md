# 未解决项与运行注意

## 功能阻塞

无。发布、数据库 current、Qdrant Alias、Search、QA、批量 QA 和 Web → Worker 连接均已实测闭环。

## 运行注意

本机使用 CPU 加载 `bge-large-zh-v1.5` 与 `bge-reranker-v2-m3`；后端重启后首次双模型加载可能超过 420 秒。当前进程已经完成预热，热态 Search 为 1.469 秒。启动器默认不在启动阶段强制多 GB 模型预热，以避免低内存机器启动失败；此项不伪报为热态性能，也不影响当前已预热实例的功能可用性。
