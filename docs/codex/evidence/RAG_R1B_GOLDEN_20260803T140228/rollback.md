# 回滚方案

- 数据库影响：无；未执行迁移、写库、全表更新、seed、激活或正式数据访问。
- 运行态影响：无；未创建 snapshot，未切换 alias，未修改 release 状态，未发布。
- 提交后回滚：在本工作树对最终提交执行 `git revert <最终提交哈希>`，可一次性恢复本任务全部受版本控制文件，并保留可审计回滚提交。
- 提交前恢复：只可逐个精确恢复本任务登记文件；新增文件的删除需按项目规则再次取得人工确认，不执行批量删除。
- 证据恢复：审核包和证据文件与代码同一提交，随 `git revert` 一并回滚。
