# 项目执行规则

1. 全部回答使用中文简体。
2. 禁止批量删除文件或目录。
3. 禁止使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
4. 删除文件时只能一次删除一个明确路径的文件；如需批量删除，停止操作并请用户手动处理。

## UI 优化强制前置规则

凡涉及前端页面新建、重做、布局调整、视觉优化、截图复刻、响应式修复或组件排版，必须在分析和编辑前：

1. 加载并遵循项目执行技能 `.agents/skills/p6-ui-rebuild/SKILL.md`。
2. 按执行技能要求读取 `docs/P6_UI优化前必读统一SKILL.md` 和 `references/ui-standards.md`。
3. 检查本次目标参考图、当前页面代码、真实 API 和后端能力。
4. 按页面关键词定向查阅两份源 Word 文档对应章节，不重复无目的通读全文。
5. 未完成技能中的前置检查和验收清单，不得开始修改或宣称任务完成。

参考图决定视觉结构，真实项目和真实 API 决定功能边界。禁止为了视觉复刻伪造数据或未部署能力。

## P6 UI 规范文件地图

1. `.agents/skills/p6-ui-rebuild/SKILL.md`
   - Codex 自动执行入口。
   - 每次 UI 任务必须首先读取。
   - 负责前置检查、实施步骤、验收和输出规则。
2. `docs/P6_UI优化前必读统一SKILL.md`
   - 项目完整总规范和页面级索引。
   - 每次任务读取通用章节，并重点读取当前页面对应章节。
3. `.agents/skills/p6-ui-rebuild/references/ui-standards.md`
   - 精简视觉、数据、交互和验收硬性标准。
   - 实现前读取，验收时逐项复核。
4. `docs/P6_UI优化前必读统一SKILL_打包版/SKILL.md`
   - 与 `docs/P6_UI优化前必读统一SKILL.md` 内容相同的分发备份。
   - 文件内容相同时无需重复读取；仅用于复制、移交或恢复。
5. `docs/智能运营分析项目_UI优化参考图与当前功能差异分析说明文档_v20260621.docx`
   - 用于确认目标功能、当前差异、数据支撑和缺口。
6. `docs/智能运营分析项目_P6_UI与功能排版优化落地方案_Codex版_v20260622.docx`
   - 用于确认 UI-0 至 UI-4 阶段、组件/API 复用、安全边界和交付要求。
7. 本次任务目标参考图、用户标注截图和最新文字要求
   - 用于确认当前页面的最终结构、位置、尺寸和局部调整。

## 规则冲突优先级

出现描述差异时，按以下顺序执行：

1. 当前任务中用户最新、明确的要求。
2. 数据真实性、安全性、只读 SQL、RAG 降级和禁止伪造能力等不可突破规则。
3. 根目录 `AGENTS.md`。
4. `.agents/skills/p6-ui-rebuild/SKILL.md`。
5. `docs/P6_UI优化前必读统一SKILL.md` 与 `references/ui-standards.md`。
6. 两份历史 Word 方案和较早截图说明。

展示层可按用户最新要求调整，例如正常业务页面可以隐藏技术型数据来源标签；但不得把 mock、fallback、派生结果或未部署能力冒充真实生产能力。
