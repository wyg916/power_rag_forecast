# AGENTS.md — 智能运营分析项目强制执行规则

## 1. 适用范围
本文件适用于本仓库内的所有分析、修改、测试、迁移、构建和文档任务。开始任何工作前，必须先读取：
- `docs/codex/PROJECT_BASELINE_SUMMARY.md`
- `docs/codex/MASTER_ENGINEERING_RULES.md`
- 当前任务对应的 `docs/codex/tasks/*.md`

## 2. 核心目标
在保证正确性、安全性和可回滚性的前提下，以最小必要上下文、最小修改范围和最少重复扫描完成任务。当前第一目标是把项目从“开发演示”推进到“可稳定本地运行”，不是新增大量功能。

## 3. 文件系统红线
- 默认只允许修改项目根目录及其子目录；项目建议位于 `E:\智能运营分析项目`。
- **C 盘默认只读**。尽量不要在 C 盘新增、删除、移动、覆盖项目文件，尽量不要修改注册表、系统服务、全局环境变量或系统级配置。
- 禁止执行递归清理或破坏性命令，包括但不限于：`git clean -fd/-fdx`、`rm -rf`、`rmdir /s /q`、`del /s /q`、无范围限制的 PowerShell `Remove-Item -Recurse -Force`。
- 删除、批量移动、覆盖现有文件前，必须列出精确路径、原因、影响和回滚方法，并停止等待用户确认。
- 必要时可以进行必要的操作，但在执行时需要让我手动确认。

## 4. 备份与回滚红线
- 每个任务开始前必须创建可验证检查点；备份目录必须位于项目盘，不得写入 C 盘。
- 至少保存：`git status`、`git diff`、未跟踪文件清单、修改文件清单、关键文件哈希和数据库影响说明。
- 涉及数据库迁移、模型状态、预测事实表或配置变更时，必须额外提供迁移回滚或数据恢复方案。
- 未证明可以回滚，不得开始高风险修改。

## 5. Token/上下文控制
- 不重复全仓扫描；优先使用精确文件、符号和目录范围。
- 不在每个任务中重复读取大体量历史文档；使用本仓库的基线摘要和任务文件。
- 每次只解决一个任务包；禁止顺手重构无关模块。
- 输出保持结构化且简洁：计划、修改、测试、风险、回滚、剩余问题。
- 默认不生成大段重复代码或重复粘贴完整日志；日志保存到证据文件，回复只给摘要和路径。

## 6. 修改范围与停止条件
出现以下任一情况必须停止并请求用户确认：
- 需要写入 C 盘、修改系统级设置或全局安装软件；
- 需要删除、覆盖、批量移动已有文件；
- 需要反序列化未经授权的模型二进制；
- 需要连接正式数据库、使用真实密钥或执行外部网络调用；
- 需要执行不可逆数据库操作或全表更新/删除；
- 预计修改超过 20 个文件或 1000 行有效代码；
- 实际情况与任务基线明显冲突；
- 无法建立备份或无法给出回滚路径。

## 7. 数据与模型规则
- GET/查询接口不得产生 seed、激活、同步或其他写副作用。
- 不得把 seed/demo/fallback/历史数据伪装为实时真实数据。
- 所有前端业务页面不得展示“模拟数据”“真实数据”“模拟入库”等数据来源分类文案，也不得直接展示 `data_source`、`source_type`、`is_simulated` 等技术追溯字段；前端应使用中性的业务名称、业务时间、更新时间和状态语义呈现数据。
- 前端隐藏来源分类不等于删除追溯信息：数据库、后端领域服务、API 契约、日志和审计记录仍必须完整保留来源、模拟标识、批次、生成时间和场景等元数据，供权限受控的排障与审计使用。
- 页面所需数据缺失时，只允许按真实业务字段、约束和计算规则生成受控数据，并严格经过“受控生成程序 → PostgreSQL → 后端领域服务/Repository → API → 前端状态与组件”链路；禁止在前端生成、硬编码或用接口失败后的成功态 fallback 补齐业务数据。
- 前端仍必须如实呈现 Loading、Empty、Error、Stale、Unauthorized、Forbidden 等状态以及业务时间/更新时间；不得把过期、测试或受控生成结果明示为实时设备采集、真实交易执行或真实结算结果。
- 特征缺列、额外列、顺序、类型、时区或 schema hash 不一致时必须 fail-closed，禁止静默补 0。
- 未经准入验证和人工确认，Candidate 不得自动晋升 Active。
- 预测、报告、策略和 AI 工具必须共享可追溯 `run_id` 与来源元数据。

## 8. 测试与交付
- 每项修改必须运行最小相关测试；高风险任务还必须运行回归、失败回滚和幂等测试。
- 不得把“代码存在”“页面可启动”或“历史测试通过”表述为当前闭环已完成。
- 每项任务结束时更新 `docs/codex/TASK_STATUS.md`，并生成任务证据目录。
- 未通过验收标准时，明确标记未完成，不得伪造通过结果。

# 项目执行规则

1. 全部回答使用中文简体。
2. 禁止批量删除文件或目录。
3. 禁止使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
4. 删除文件时只能一次删除一个明确路径的文件；如需批量删除，停止操作并请用户手动处理。

## UI 优化强制前置规则

凡涉及前端页面新建、重做、布局调整、视觉优化、截图复刻、响应式修复或组件排版，必须在分析和编辑前：

1. 加载并遵循项目执行技能 `.agents/skills/p6-ui-rebuild/SKILL.md`。
2. 按执行技能要求读取 `docs/P6_UI优化前必读统一SKILL.md` 和 `references/ui-standards.md`。
3. 检查本次目标参考图、当前页面代码、真实 API 和后端能力。
4. 按页面关键词定向查阅两份源 Word 文档对应章节，不重复无目的通读全文。
5. 未完成技能中的前置检查和验收清单，不得开始修改或宣称任务完成。

参考图决定视觉结构，真实项目和真实 API 决定功能边界。禁止为了视觉复刻伪造数据或未部署能力。
